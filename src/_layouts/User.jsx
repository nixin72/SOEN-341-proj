import React from 'react';
import ReactDOM from 'react-dom';


export default function User(input) {
    this.userName = input;
  }


//I dont think react works in this way
//but not sure how user could possibly be returned as a string

